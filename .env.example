# Relay server port (WebSocket)
RELAY_PORT=9090

# Web server port (serves the game)
WEB_PORT=8080

# Public URL of the relay server (players connect to this)
# Set this to your domain, e.g. wss://yourdomain.com:9090
RELAY_URL=ws://localhost:9090
