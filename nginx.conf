server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Required headers for Godot HTML5 exports (SharedArrayBuffer support)
    add_header Cross-Origin-Opener-Policy "same-origin" always;
    add_header Cross-Origin-Embedder-Policy "require-corp" always;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets (short cache to allow updates via hard refresh)
    location ~* \.(wasm|pck|js|worker\.js)$ {
        add_header Cross-Origin-Opener-Policy "same-origin" always;
        add_header Cross-Origin-Embedder-Policy "require-corp" always;
        # Use must-revalidate so hard refresh (Ctrl+Shift+R) fetches fresh files
        add_header Cache-Control "public, max-age=3600, must-revalidate";
    }
}
